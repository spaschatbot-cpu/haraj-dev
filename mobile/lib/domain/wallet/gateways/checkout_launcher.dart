/// فتح صفحة الدفع خارج التطبيق.
///
/// منفذ في النطاق لا استدعاء مباشر لحزمة: النطاق يبقى Dart صافياً، وتُختبر
/// رحلة الشحن كاملة بلا متصفّح. وأهمّ من ذلك أن هذا هو **كل** ما يفعله التطبيق
/// عند البوابة — يفتح عنواناً. لا استقبال لنتيجة، ولا قراءة لما يعود.
abstract interface class CheckoutLauncher {
  /// يفتح العنوان في متصفّح خارجي. يرجع `false` إن تعذّر الفتح.
  ///
  /// لا يرمي: تعذّر الفتح ليس فشلاً في الشحن — النيّة مكتوبة عند الخادم،
  /// والعميل يستطيع المحاولة ثانية بالمرجع نفسه.
  Future<bool> open(String url);
}
