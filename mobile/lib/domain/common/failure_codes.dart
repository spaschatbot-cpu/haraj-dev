/// رموز الأخطاء التي **يتفرّع** عليها التطبيق برمجياً.
///
/// الرمز هنا لا يُعرض ولا يُترجم — الرسالة العربية تأتي من الخادم دائماً
/// (انظر `failure.dart`). هذه القائمة موجودة لأن ثلاث حالات تحتاج سلوكاً لا
/// نصاً:
///
/// * انتهاء رمز الوصول ← تجديد صامت وإعادة المحاولة مرة واحدة.
/// * خفض المزايدة ← حوار تأكيد يذكر المبلغين ثم إعادة الإرسال بتأكيد.
/// * تجاوز حدّ المعدّل ← تعطيل زرّ الإرسال حتى انقضاء المهلة.
///
/// أي رمز آخر يصل من الخادم يُعرض برسالته ولا يحتاج ذكراً هنا. لذلك القائمة
/// قصيرة عمداً، ولا تُحوَّل إلى مرآة لكل رموز الفيز 007.
library;

abstract final class FailureCodes {
  /// رمز الوصول منتهٍ — يُجدَّد ولا يُعرض للمستخدم.
  static const String tokenExpired = 'TOKEN_EXPIRED';

  /// المزايدة أقل من الحالية وتحتاج تأكيداً صريحاً (409 ثم تأكيد).
  ///
  /// القيمة هي رمز الخادم حرفياً (`BiddingError.code` في
  /// `apps/bidding/services.py`)، لا صيغة مشتقّة منه. رمزٌ نكتبه بصياغتنا
  /// يجعل الحوار لا يظهر أبداً أمام الخادم الحقيقي، ويظهر في كل اختبار
  /// كتبناه نحن — وهذا بالضبط ما يجعل J7 (نفس الرفض المُعدَّد في القناتين)
  /// يسقط عند العميل لا عندنا.
  static const String bidLowerNeedsConfirmation = 'lower_needs_confirm';

  /// تجاوز حدّ المعدّل.
  static const String rateLimited = 'RATE_LIMITED';
}
