/**
 * قائمة المزادات — محالة إلى الجذر، لا صفحة.
 *
 * المزاد أسبوعي واحد بحالات (قريباً · نشط · منتهي)، فالعميل لا يختار مزاداً من
 * قائمة بل يتصفّح مركباته مباشرة في `/` التي صارت اسمها «المزادات». قائمة
 * المزادات كوحدة للإدارة (الفيز 009)، لا لقناة العميل.
 *
 * والملف باقٍ لا محذوف لسبب واحد: روابط قديمة مشارَكة أو مفهرَسة على
 * `/auctions` تهبط على المزادات لا على 404.
 */

import { redirect } from "next/navigation";

export default function AuctionsPage() {
  redirect("/");
}
