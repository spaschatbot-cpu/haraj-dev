# نسخة للتطوير المحلي. الأسرار لا تدخل المستودع أبداً (المادة ٥-٣).

# الخلفية كما يصل إليها *الخادم* — لا يُرسل هذا إلى المتصفح أبداً.
BACKEND_URL=http://127.0.0.1:8000

# يظهر في لافتة كل بيئة غير الإنتاج (المادة ٥-٦، T1006).
ENVIRONMENT_NAME=development
