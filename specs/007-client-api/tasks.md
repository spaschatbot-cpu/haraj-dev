# تاسكات الفيز 007 — API العميل

المراجع: [spec.md](spec.md) · [الدستور](../../.specify/memory/constitution.md)
يعتمد على: الفيز 002 و005 و006 مغلقة.

---

## المجموعة أ — المصادقة

### ✅ T601 — إرسال والتحقق من OTP
**الملفات:** `backend/apps/accounts/{errors,otp,sms,tokens,authentication,checks}.py`،
`services.py`، `api/{auth,serializers,urls}.py`، هجرة `0003`
**المطلوب:** إرسال رمز، التحقق، إصدار رمزي وصول وتحديث. الرمز محدود الصلاحية،
ومحدود المحاولات، ولا يُعاد في الاستجابة أبداً.
**القبول:** اختبار: صحيح، منتهٍ، محاولات زائدة، رمز مُعاد استعماله.

**ما نُفِّذ:** ٢٦ اختباراً في `apps/accounts/tests/test_auth_otp.py`، والأربعة
المطلوبة منها صراحةً. الحزمة كلها ٧٣٦ اختباراً خضراء على PostgreSQL 17.11
بإعدادات `test` الوارثة من `prod`.

**قرارات وأسبابها:**

* **رموز معتمة مخزَّنة، لا JWT.** المنصة تحرّك فلوساً، وفي v1 كان هناك مسار
  استيلاء على حساب. JWT لا يُسحب قبل انتهائه؛ الصف يُسحب بتحديث واحد.
* **الرمز يُخزَّن كبصمة SHA-256، والرموز كذلك.** نسخة من القاعدة على لابتوب أو
  استعلام دعم ملصوق في محادثة لا يسلّم أحداً مفتاحاً يعمل.
* **دوران عند كل استعمال + كشف إعادة الاستعمال.** رمز تحديث يظهر بعد صرفه يعني
  أن طرفين يحملانه، فتُسحب السلسلة كلها. خسارة جلسة أرخص من إبقاء مسروقة حية.
* **الاسم يُطلب قبل استهلاك الرمز.** رفض بعد الاستهلاك يكلّف العميل رسالة جديدة
  ودقيقة إضافية لخطأ ما زال أمامه على الشاشة.
* **حارس مزوّد الرسائل فحصُ نشر لا `raise` عند الاستيراد.** `settings/test.py`
  يرث `prod.py` عمداً؛ حارس عند الاستيراد هناك يمنع إعدادات الاختبار من التحميل
  أصلاً — قاعدة عن الإنتاج تكسر الـCI.

**عطلان حقيقيان اكتُشفا بالتشغيل، لا بالقراءة:**

1. **الرفض داخل `transaction.atomic` يمسح الكتابة التي يجب أن تعيش.** عدّ
   المحاولة الخاطئة كان يُرجَّع مع الاستثناء، فميزانية الخمس محاولات لا تنفد
   أبداً — أي لا ميزانية. الحلّ: الكتلة تقرّر وتكتب، والرفض يُرفع بعد إغلاقها.
   نفس العطل كان في سحب سلسلة الرموز عند كشف إعادة الاستعمال: العميل يقرأ 409
   واللص تبقى جلسته حية.
2. **`check --deploy` كشف أن المخطط لا يعرف المصادقة الجديدة.** بلا
   `OpenApiAuthenticationExtension` كان كل عميل مولَّد (T702 وT1002) سيُشحن بلا
   طريقة لإرفاق رمز. أُضيف المخطط الأمني `bearerAuth`.

**ما لم يُنفَّذ عمداً:** تحديد المعدّل (T602)، وتمييز فشل المزوّد (T603 — الفاصل
`SmsSendFailed` موجود ليُبنى عليه)، وتغيير الجوال (T604).

### ✅ T602 — تحديد معدّل على **كل** مسارات OTP
**المطلوب:** حدّ واحد مشترك يغطي كل مسار يرسل رمزاً — التسجيل، الدخول، تغيير
الجوال، استعادة الحساب.
**القبول:** G6؛ وفحص نصّي يؤكد ألّا مسار إرسال بلا الحدّ.
**لماذا:** مسار واحد بلا حدّ يكفي لجعل حساب الرسائل بوابة مجانية.

**ما نُفِّذ:** `apps/accounts/throttling.py` — ثلاثة حدود في مجموعتين مسمّاتين
(`OTP_SEND_THROTTLES` و`OTP_VERIFY_THROTTLES`)، معلَّقة على المسارين في
`api/auth.py`. و17 اختباراً في `tests/test_otp_rate_limit.py`. الحزمة كلها
**753 اختباراً خضراء** على PostgreSQL 17.11 بإعدادات `test` الوارثة من `prod`.

**قرارات وأسبابها:**

* **حدّان على الإرسال لا واحد، وكلٌّ يسدّ ثغرة الآخر.** حدّ على الرقم وحده
  يُهزَم بالمشي على خطة الترقيم — طلب واحد لكل رقم من ألف رقم يكلّف ألف رسالة
  ولا يوقظ أي عدّاد لرقم. وحدّ على العنوان وحده يترك شبكة أجهزة تصوّب كلها على
  جوال ضحية واحدة. الافتراضي: 5/ساعة للرقم، 20/ساعة للعنوان.
* **حدّ ثالث على التحقق.** ميزانية الخمس محاولات لكل *رمز*، فمن يملك قائمة
  أرقام يأخذ خمس محاولات مجانية على كلٍّ منها ولا يوقظ شيئاً. 30/ساعة للعنوان.
* **المعدّلات في `OTP_THROTTLE_RATES` لا في `DEFAULT_THROTTLE_RATES`.**
  `settings/test.py` يُفرغ إعداد DRF عمداً حتى لا تتقاسم 736 اختباراً عدّاداً
  ساعياً واحداً وتفشل بترتيب تشغيلها. فالحدود تقرأ إعدادها الخاص، والنطاق
  الغائب من القاموس يعني «مطفأ هنا» لا `ImproperlyConfigured` عند أول طلب —
  واختبارات T602 تشعله لكل حالة بـ`override_settings` وذاكرة خاصّة بها.
* **الإطفاء آمن لأنه صاخب.** `accounts.E002` يرفض بيئة منشورة بلا معدّلات.
* **ذاكرة مشتركة، وإلا فالحدّ كذبة.** الحدّ يعيش في الكاش؛ تحت gunicorn كل عامل
  يملك ذاكرته، فأربعة عمّال يجعلون «5/ساعة» تساوي 20/ساعة ولا شيء في الإعدادات
  يقول ذلك. أُضيف `CACHES` على Redis (قاعدة 2 — 0 و1 لـCelery، و`FLUSHDB` على
  طابور لا يجوز أن يصفّر حدود الناس)، و`accounts.E003` يرفض بيئة منشورة على
  ذاكرة لكل عملية.
* **طلب بلا رقم لا يُحاسَب.** `get_cache_key` يرجع `None` — وإلا لأمكن إفراغ
  ميزانية دلو `None` مشترك بإرسال هراء. مُختبَر.

**عطل حقيقي مُنِع بالتصميم:** لو عُلِّقت الحدود على `DEFAULT_THROTTLE_CLASSES`
لكانت `test.py` أطفأتها في الإنتاج أيضاً عبر الوراثة المقصودة — الفحص النصّي
كان سيمرّ والحدّ غير موجود.

**الفحص النصّي:** `ops/checks/one_otp_rate_limit.py` — قاعدتان: (١) أي استدعاء
لـ`send_verification_code` خارج `services.py` يجب أن يكون داخل كلاس يعلن
`OTP_SEND_THROTTLES`، ومثله مسارات صرف الرمز مع `OTP_VERIFY_THROTTLES`؛
(٢) الحدود تُكتب بالمجموعة المسمّاة لا بيد اليد، فالحدّ الرابع يصل كل المسارات
دفعة واحدة. مُثبَت بزرع خمس مخالفات مختلفة تحت كل قاعدة، وخطوة مستقلة في
`ci.yml`.

### ✅ T603 [P] — فشل الإرسال يُميَّز عن خطأ الكود
**المطلوب:** فشل مزوّد الرسائل (رصيد، عطل) يُرجع رمزاً مميّزاً ويُسجَّل بحيث
يظهر في شاشة الصحة.
**القبول:** اختبار بمزوّد مُعطَّل → رمز واضح لا «حدث خطأ».
**لماذا:** «تعذّر إرسال رمز التحقق» في v1 كان يعني نفاد رصيد الرسائل، وكان يُشخَّص
كل مرة من الصفر.

**ما نُفِّذ:** `VerificationCodeUndeliverable` بالرمز `sms_undeliverable`،
ونموذج `SmsFailure` وهجرة `0004`، و`recent_sms_failures()`، وفحص `sms` في
`/health`. 13 اختباراً في `tests/test_sms_failure.py`.

**قرارات وأسبابها:**

* **503 لا 409.** كل رفض آخر في هذا التطبيق 409 — «طلبك حُكم عليه ورُفض»، وهو
  ما يدعو التطبيق لعرض الجملة والتوقّف. عطل المزوّد ليس حكماً على الطلب، و503
  تعني «ليس الآن، أعد المحاولة» — وتُبقي عطلنا خارج ميزانية أخطاء العميل.
  لتحقيقه أُضيف `status_code` على `DomainError` (افتراضه 409)، ويقرأه
  `apps/core/exceptions.py` وحده.
* **الفشل يُكتب خارج المعاملة — وهذا هو التاسك.** الإرسال يحدث داخل الكتلة
  الذرّية عمداً، فسقوطه يُرجّع صفّ الرمز غير المُرسَل. وسجل الفشل لو كُتب داخل
  نفس الكتلة لرجع معها، فيختفي الدليل في اللحظة التي يُطلب فيها. الكتلة تسقط
  وتُرجَّع، **ثم** نكتب. نفس شكل العطل الذي وجده T601 في
  `check_verification_code`، ومكتوب في `services.py` حتى لا يصير نمطاً.
* **كلام المزوّد يُحفظ ولا يُفسَّر** (المادة ٢-٣): `reason` نصّ كما جاء، لا
  حالة مُعرَّفة عندنا.
* **`/health` يقول degraded لا down.** عطل الرسائل ليس سبباً لسحب الخدمة من
  موزّع الأحمال — ومسار الدخول يجيب 503 بنفسه لمن يسأله.
* **العدد لا العلم فقط:** `failures_last_hour` — فشل واحد دقيقة سيّئة، وأربعون
  في ساعة رصيدٌ نفد في وقت يمكن معرفته.

### ✅ T604 — تغيير الجوال بتأكيد الرقمين
**القبول:** اختبار: تأكيد القديم وحده لا يكفي؛ الجديد وحده لا يكفي.

**ما نُفِّذ:** `start_phone_change` و`confirm_phone_change` في `services.py`،
ونقطتان: `auth/phone/change/` و`auth/phone/change/confirm/`. 20 اختباراً في
`tests/test_phone_change.py`.

**قرارات وأسبابها:**

* **رمزان لأن مسار v1 احتاج واحداً.** كان إثبات الرقم الجديد وحده كافياً، فمن
  وصل إلى جلسة مفتوحة — لابتوب مشترك، جوال سُلِّم غير مقفل — نقل الحساب إلى رقم
  يملكه، وتوقّف رقم صاحبه دون أن يُسأل. اشتراط القديم يجعل السرقة تحتاج أن تنجو
  من رنين جوال المالك الحقيقي.
* **الرمزان يُحكَم عليهما قبل أن يُصرَف أيّهما.** الاستدعاء المزدوج للفحص
  المفرد كان سيصرف الرمز الصحيح عند خطأ في الثاني، فيحتاج العميل رسالتين
  جديدتين — وT602 يحدّها بخمس في الساعة، فخطآن يقفلانه ساعة. هنا يُقفل الصفّان
  ويُقارَنان، وتُحسب المحاولة على الخاطئ وحده، ولا يُصرَف أحدهما إلا بنجاحهما.
* **الرفض لا يقول أيّ رمز كان خاطئاً.** من يملك أحد الجوالين كان سيختبر النصف
  الآخر محاولةً محاولة. الزوج يفشل معاً.
* **الرقم المتروك يُقرأ من الرمز لا من الطلب.** جسم يسمّي الرقم الحالي يجعل
  النقطة وسيلةً لنقل حساب غيرك — وهو شكل الاستيلاء نفسه. مُختبَر.
* **رقم مسجَّل على حساب آخر يُرفض قبل إرسال أي رسالة**، وإلا رنّ جوال غريب برمز
  لتغيير لم يطلبه — تحرّش ومسار تصيّد. ويُعاد الفحص داخل القفل، لأن أحداً قد
  يُنهي التسجيل بين الخطوتين.
* **كل الجلسات تُلغى بعد التغيير.** لو كان التغيير استيلاءً فهذا ما يُنهيه؛ ولو
  كان المالك فثمنه تسجيل دخول واحد على أجهزته. المقايضة ليست متقاربة.
* **الرفع بعد إغلاق المعاملة** — نفس درس T601: استثناء داخل الكتلة التي حسبت
  المحاولة يأخذ الحساب معه، وميزانية لا تنفد ليست ميزانية. مُختبَر بإفراغ
  الميزانية فعلياً.

**ما أثبته الفحص النصّي:** النقطتان الجديدتان مرّتا على
`ops/checks/one_otp_rate_limit.py` لأنهما تحملان المجموعتين المسمّاتين — وهذا
هو المسار الرابع الذي كُتب بعد الحدّ، والذي وُضع الفحص من أجله بالضبط.

---

## المجموعة ب — الملف الشخصي

### ✅ T605 [P] — عرض وتعديل الملف
**المطلوب:** الحقول الموجودة فعلاً فقط. أي حقل غير موجود في الجدول يُرفض عند
التحقق، لا عند الحفظ.
**القبول:** اختبار: حقل مجهول → 400 واضح، لا 500.

### ✅ T606 [P] — الهوية الوطنية: تثبيت مرة على القيمة الصحيحة
**القبول:** اختبار: قيمة خاطئة مخزَّنة يمكن تصحيحها؛ صحيحة لا يمكن تغييرها.

### ✅ T607 [P] — ملف الشركة والعنوان الوطني (ZATCA)
**المطلوب:** الحقول المطلوبة إلزامية للشركات الجديدة، والقديمة معفاة بتاريخ قاطع
قابل للضبط.
**القبول:** اختبار للحالتين.

**ما نُفِّذ (T605–T607):** `apps/accounts/api/profile.py` بثلاث نقاط،
و`identity.py`، وثلاث دوال في `services.py`. 32 اختباراً في
`tests/test_profile_api.py`.

**قرارات وأسبابها:**

* **قائمة سماح مسمّاة لا مشتقّة من النموذج** (`EDITABLE_PROFILE_FIELDS`). حقل
  يُضاف للجدول غداً — علم، رصيد، ملاحظة موظف — يجب ألّا يصير قابلاً للتعديل من
  الجميع لمجرّد وجوده. في v1 كانت النقطة تمرّر جسم الطلب إلى `.update(**body)`،
  فخطأ مطبعي خطأُ قاعدة بيانات ومفتاح مختار جيداً تصعيدُ صلاحية.
* **الحقل المجهول يُرفض لا يُهمَل.** الافتراضي في DRF إهماله، فيتحوّل خطأ العميل
  إلى لا-عملية صامتة: العميل يضغط حفظ، لا يرى خطأً، والقيمة لا تتغيّر أبداً.
* **T606 نصفان، والثاني هو الجديد.** الهوية *الصحيحة* تُثبَّت للأبد لأن الحساب
  يحمل التزامات تخصّ من يسمّيه ذلك الرقم؛ و*الخاطئة* تُصحَّح، لأن من أخطأ رقماً
  يجب أن يصحّح نفسه — في v1 كانت أول قيمة تُكتب نهائية، فتصحيح الخطأ كان يعني
  طلب الدعم أن يعدّل القاعدة، وتعديل الدعم لأعمدة الهوية مشكلة بذاتها.
  و`identity.py` هو تعريف «الصحيح»: عشرة أرقام تبدأ بـ1 أو 2، وLuhn من اليسار
  (مساعد مكتوب لبطاقات الائتمان يخطئ هذه).
* **الخروج من حالة القابلية للتصحيح باتجاه واحد:** لا يُقبل تصحيح خطأ بخطأ آخر.
* **`national_id_verified` مشتقّ لا مخزَّن.** عمود منطقي كان مصدراً ثانياً
  للحقيقة عن نفس القيمة، ويختلف عنها أول مرة يُصحَّح رقم في القاعدة مباشرةً.
* **إعفاء الشركات القديمة (T607) مسار ترحيل لا تساهل.** ثلث شركات v1 بلا رمز
  بريدي؛ رفض حفظ اسم ممثل مصحَّح حتى تُنتج «حياً» يقفل حسابات عاملة خارج ملفها.
  والشركة المعفاة تبقى غير قابلة للفوترة — وذلك رفض يخصّ الفاتورة لا هذا النموذج.
* **الحساب يصير حساب شركة بامتلاكه ملفاً**، لا بادّعاء ذلك في جسم الطلب.

---

## المجموعة ج — المزادات والمزايدة

### ✅ T608 — قائمة المزادات والمركبات
**المطلوب:** ترقيم وترشيح وبحث في SQL، بحدود على كل مُعامل.
**القبول:** ألف مركبة → أقل من 300 مللي ثانية؛ لا استعلام O(n²).

### ✅ T609 [P] — تفاصيل المركبة
**المطلوب:** يستعمل مُسلسِل الكرت الواحد من الفيز 005.
**القبول:** الحقول مطابقة للقائمة تماماً — اختبار يقارن المجموعتين.

**ما نُفِّذ (T608 + T609):** `apps/auctions/api/` بخمس نقاط، فوق
`listing.py` و`cards.py` من الفيز 005. 17 اختباراً في `tests/test_browse_api.py`.

**قرارات وأسبابها:**

* **سقف على كل مُعامل.** `MAX_PAGE_SIZE = 100`؛ v1 كان يأخذ أي `limit` يصله،
  فـ`?limit=100000` كان مسحاً كاملاً للجدول يطلبه أي عميل. والترقيم رخيص فقط
  لأن الصفحة صغيرة.
* **الترشيح قبل العدّ والتقطيع، في SQL.** أول نسخة كتبتها كانت ترشّح المزادات
  **بعد** الترقيم — فـ`total` يصير عدد الصفحة لا عدد النتيجة، وهي كذبة تظهر في
  الصفحة الثانية. صار الترشيح `WHERE`، واختبار يثبت أن الإجمالي يعدّ النتيجة.
* **قاعدة الظهور تُطبَّق أولاً ولا يُلتفّ حولها:** من يسمّي حالة لا يراها يحصل
  على صفحة فارغة لا على تسريب. مُختبَر.
* **الرقم في مربع البحث يعني رقم اللوت** — لأن هذا ما يعنيه لكل من يستعمل
  النظام. و`icontains` لا فهرس نصّي كامل: في هذا الحجم صادق، وفهرس وهمي كان
  سيُخفي اليوم الذي يتوقّف فيه عن الكفاية.
* **404 لا 403** للمركبة والمزاد غير المرئيين: 403 يؤكّد وجود الصفّ، وهذا يكفي
  لتعداد مزاد قبل فتحه.
* **`with_vehicle_counts` دالة واحدة** تستعملها القائمة والتفصيل، فلا يختلفان
  على عدد سيارات المزاد.
* **التكلفة مقيسة لا مُدَّعاة:** اختبار يعدّ الاستعلامات ويثبت أن صفحة من
  عشرين تكلّف ما تكلّفه صفحة من واحدة — يسقط عند خمس سيارات لا عند خمسة آلاف.

### ✅ T610 — نقاط المزايدة
**المطلوب:** وضع، خفض بتأكيد (409 ثم تأكيد)، سحب، مزايداتي. كلها عبر
`bidding.services`.
**القبول:** اختبار لكل سبب رفض يؤكد الرمز والرسالة العربية.

### ✅ T611 [P] — تحديد معدّل على المزايدة
**القبول:** تجاوز الحدّ → 429 بلا فقد مزايدات ضمن الحدّ.

**ما نُفِّذ (T610 + T611):** `apps/bidding/api/` بثلاث نقاط، و`throttling.py`،
و`checks.py`. 22 اختباراً في `tests/test_bidding_api.py`.

**قرارات وأسبابها:**

* **لا نقطة تسرد المزايدات *على* مركبة — بالغياب لا بالصلاحية.** خاصية المزاد
  المغلق كلها أن المزايدين لا يرون أرقام بعضهم؛ صلاحية يمكن لأحد أن يرخيها
  لاحقاً ليست ضماناً. واختبار يؤكّد أن المسار غير موجود أصلاً.
* **المبلغ نصّ من الطلب إلى `Decimal`.** `DecimalField` كان سينشر
  `type: number` في المخطط، وعميل JavaScript يقرأ `55000.50` يكون قد خسرها
  لعائم قبل أن يراها أي كود عندنا (المادة ٣-٢). ونمط يرفض `1e5` و`55_000` قبل
  أن يصلا إلى المُحلِّل، فالرفض خطأ حقل مسمّى لا استثناء.
* **الحدّ على الحساب لا على العنوان.** المزايدون يتشاركون مكاتب وتشاركون NAT،
  فحدٌّ على العنوان يجعل ثاني مزايد في شركة ينتظر الأول. والنقطة تتطلّب مصادقة
  فالحساب موجود دائماً.
* **لماذا حدّ على المزايدة أصلاً:** ليس التكلفة — المزايدة لا ترسل رسالة. بل
  (١) سكربت يسابق الإغلاق يصنع خمسين معاملة يقف خلفها بقية المزاد، و(٢) كل رفض
  يسمّي أرقام صاحبه (`available`، `required`) لأن العميل يستحق أن يعرف — وبلا
  حدّ تصير تلك الصراحة عرّافاً يُقرأ برصيد ريال في كل مرة.
* **`bidding.W001` تحذير لا خطأ**، بخلاف حدود OTP: مسار إرسال بلا حدّ فاتورة
  غيرنا علينا، ومسار مزايدة بلا حدّ دقيقة أخيرة مزدحمة — جادّ، لا «امنع النشر».

**المخطط:** صار **23 مساراً** بعد أن كان 15 — المزادات والمركبات والمزايدة
دخلت العقد، وهي ما كان ينقص 008 و011.

---

## المجموعة د — المحفظة والمال

### ✅ T612 — المحفظة بدلائها مفصَّلة
**المطلوب:** متاح، محجوز لمزاد (مع اسم المزاد)، مقفول لمستحقات (مع رقم الفاتورة).
**القبول:** G5 — المجموع يطابق الدفتر؛ واختبار يؤكد ظهور سبب كل حجز.
**لماذا:** «رصيدك 10,000» بلا تفصيل جعل عملاء v1 يظنّون فلوسهم متاحة وهي محجوزة.

### ✅ T613 [P] — كشف الحركات
**المطلوب:** من القيود مباشرةً، مرقَّم، بوصف عربي لكل نوع معاملة.
**القبول:** كل نوع معاملة له وصف؛ لا نوع يظهر بمفتاحه الإنجليزي.

### ✅ T614 — الشحن بالبطاقة
**المطلوب:** المبلغ من الخادم لا من الطلب. سجّل النية قبل التحويل للبوابة.
**القبول:** اختبار: تلاعب بالمبلغ في الطلب → مرفوض.
**لماذا:** تلاعب بمبالغ التأمين كان من نتائج اختبار الاختراق في v1.

### ✅ T615 — عودة الدفع وإسنادها
**المطلوب:** الدفعة العائدة بلا هوية مستخدم → `suspense` + ملاحظة، لا إسقاط.
**القبول:** اختبار: عودة بلا معرّف مستخدم → الفلوس محفوظة وظاهرة.
**لماذا:** بوابة الدفع لا تحمل معرّف المستخدم، وهذه الفجوة أضاعت مدفوعات في v1.
**مُراجَع:** كان ناقصاً الترتيب الذي هو العطل نفسه — إشعار يسبق ظهور النية ثم
يُعاد. الإيصال المعلّق والإيداع المنسوب كانا يتقاسمان مفتاح idempotency، فالمحاولة
الثانية كانت تُعلّم النية «تمت» والفلوس في المعلّق. مغطّى الآن بـ
`TestACallbackThatBeatItsOwnIntent` وبـ`services.credit_payment`.

### ✅ T616 [P] — طلب الاسترداد
**المطلوب:** المتاح فقط. الطلب يكتب في outbox، ولا يستدعي أودو مباشرةً.
**القبول:** اختبار: مدين → مرفوض برسالة تذكر السبب والمبلغ المقفول.
**مُراجَع:** الطلب لم يكن يحجز شيئاً، فعشرة طلبات بمفاتيح مختلفة كانت تمرّ كلها
على نفس الرصيد. صار طلباً مفتوحاً واحداً لكل عميل بقيد فريد جزئي
(`one_open_refund_request_per_customer`)، ودورة الحياة تُغلق الآن عند
`refund.confirmed` — لم يكن أي كود يكتب `RefundRequestState` قبل ذلك.

### ✅ T617 [P] — المشتريات والسداد
**المطلوب:** السداد بالتحويل البنكي والرصيد فقط. **لا بطاقة للمشتريات** — بالتصميم.
**القبول:** اختبار يؤكد غياب مسار بطاقة للمشتريات.
**مُراجَع:** `money:invoice-list` لم يكن له أي اختبار — لا نجاح ولا فشل ولا ملكية.
مغطّى الآن بـ`TestTheInvoiceList` (الملكية، المسودّة، الغريب، غير المصادق).
و`pay_invoice_from_balance` صار يقرأ الفاتورة تحت قفل الصفّ بدل النسخة التي حمّلها
الـview، ويشتقّ الحالة بـ`derive_invoice_state` وحدها.

---

## المجموعة هـ — عرضية

### ✅ T618 — اختبار IDOR على كل نقطة تحمل معرّفاً
**المطلوب:** اختبار مولَّد يمرّ على كل نقطة بمعرّف مستخدم آخر ويتوقّع 403/404.
**القبول:** G3؛ ونقطة جديدة بلا فحص ملكية تُسقط الاختبار تلقائياً.

**ما نُفِّذ:** `backend/tests/test_idor.py`. **القائمة تُكتشَف من محلِّل مسارات
Django، لا تُكتب** — وهذا هو التاسك كله: قائمة مكتوبة باليد ينساها أحد، والنقطة
المنسيّة هي المضافة على عجل، وهي نفسها التي تخطّت فحص الملكية. كل مسار تحت
`api/v1/` يحمل مُعامِلاً يُستدعى بأربعة أفعال بمعرّف صفٍّ لغيره.

الإعفاءات مسمّاة ولكلٍّ سببه، **واختبار يسقط لو بقي إعفاء لمسار لم يعد موجوداً**
— وإلا ورث اسمُه الجديدُ إعفاءً لم يُقصد. والمعرّف في مسار المزايدة يسمّي
**مركبة** لا صفّ شخص، فهو معفى بوعي.

### ✅ T619 — المبالغ نصوص عشرية
**القبول:** G4 — فحص على كل الاستجابات المُولَّدة من المخطط.

**ما نُفِّذ:** `backend/tests/test_amounts_are_strings.py` — **يقرأ المخطط لا
الاستجابات.** المخطط هو العقد الذي يُبنى منه عميلان، فحقل منشور كـ`number` يصير
`double` في الـDart مهما أرسل الخادم اليوم. والمسح يفشل على نقطة لم يكتب لها
أحد اختبار استجابة — وهذا سبب فحص العقد بدل الاستجابات.

### ✅ T620 [P] — تسجيل جهاز الإشعارات
**المطلوب:** الجهاز يُربط بالمستخدم من الرمز، لا من الجسم.
**القبول:** اختبار: محاولة ربط جهاز بمستخدم آخر → مرفوضة.

**ما نُفِّذ:** نموذج `Device` وهجرة `0002`، ونقطة `api/v1/devices/`. 10 اختبارات.

**قرارات وأسبابها:**

* **لا حقل `user` في المُسلسِل، وغيابه هو التصميم.** ومحاولة تسميته تُرفض لا
  تُهمَل: عميل يظن أنه ضبط المالك ولم يفعل هو عميل لا يعرف كاتبه أن الحقل بلا
  معنى.
* **الرمز فريد على الجدول كله لا لكل مستخدم.** جهاز باع صاحبه يُعيد التسجيل
  بنفس رمز المزوّد تحت حساب جديد — ولو بقي الصفّ القديم لظلّ المالك السابق
  يستقبل تنبيهات مزايدات المالك الجديد، وهو نفس التسريب بطريق أبطأ.
* **الرمز لا يعود في أي استجابة** — هو اعتماد للإرسال لذلك الجهاز، واستجابة
  تحمله تضعه في كاش وسيط وسجل وصول وتقارير أعطال العميل. يعود `token_tail` فقط.

**أُضيف لاحقاً في T716 (الفيز 008):** `POST api/v1/devices/unregister/` — لم يكن
للخادم نقطة إلغاء تسجيل، وبدونها لا معنى لـ«إلغاء التسجيل عند الخروج» في
التطبيق. القاعدة نفسها مقلوبة: لا حقل مالك، والحذف مقصور على أجهزة المتصل —
مَن يستطيع تسمية حساب لا يقرأ تنبيهات صاحبه بل يُطفئها، ومزايدٌ لا يُبلَّغ أنه
زُوِّد عليه يخسر بلا أن يعرف أن هناك سبباً. و204 دائماً كي لا تصير النقطة
عرّافاً يكشف هل رمزٌ ما مسجَّل. ستة اختبارات معها.

### 🟡 T621 — تثبيت المخطط ومولّد العميل
**المطلوب:** `/api/schema/` صالح؛ ولّد عميل Dart منه في CI للتأكد من صلاحيته.
**القبول:** G1؛ وكسر المخطط يُسقط الـCI.

**نُفِّذ نصفه — نصف الخلفية، وهو الذي عليه معيار القبول:**

* `backend/openapi/schema.yaml` مرفوع في المستودع (15 مساراً)، ويُبنى بـ
  `--validate --fail-on-warn` بلا تحذير واحد.
* خطوة CI ترفض أي اختلاف بين المرفوع وما يولّده الكود، ووصفة `just schema`
  لإعادة توليده.
* 9 اختبارات في `backend/tests/test_schema_is_pinned.py`، ومنها أن **كل مسار
  مسجَّل تحت `/api/v1/` يظهر في المخطط** — لأن spectacular يتخطّى بصمت أي view
  لا يستطيع استبطانه، فتُشحن نقطة تعمل بـcurl ولا يراها أي عميل مولَّد.
* الأثر مُثبَت: زُرع اختلاف في الملف فسقط الاختبار، ثم أُعيد فعاد نظيفاً.

**قرار: `--fail-on-warn` لا `--validate` وحده.** spectacular *يحذّر* ولا يُخطئ
حين يعجز عن استنتاج مُسلسِل أو نوع مُعامل مسار، والتحذير يصير `dynamic` في
الـDart — عميل يُصرَّف ويحمل النوع الخطأ.

**ما لم يُنفَّذ ولماذا — سببان، كلاهما خارج هذا الفرع:**

1. **مولّد Dart لم يُعَد توجيهه.** `mobile/swagger_parser.yaml` ما زال على
   `openapi/haraj-mock.yaml`. توجيهه إلى المخطط الحقيقي يتطلّب إعادة توليد
   `lib/data/api/generated`، و**Flutter غير مثبَّت على جهاز التطوير** — وبوابة
   `mobile.yml` تشغّل `git diff --exit-code` على المولَّد، فتوجيه بلا إعادة
   توليد يُسقط البناء.
2. **الأهم: المخطط ليس جاهزاً للتثبيت بعد.** المسارات الخمسة عشر تغطّي
   المصادقة والمحفظة والفواتير فقط. **لا مزادات ولا مركبات ولا مزايدة ولا ملف
   شخصي** — تلك T605–T611 عند م2 وكلها ⬜. والمخطط المؤقّت (`haraj-mock.yaml`)
   يعرّف 16 مساراً منها `auctions` و`bids` و`profile`، و`wallet_repository_impl`
   يستهلك المولَّد منه فعلاً. التبديل اليوم يحذف عملاء تعتمد عليهم الشيفرة
   القائمة، ويعطي 008 عقداً ينقصه نصف شاشاته.

**الخلاصة للفريق:** T621 لا تُغلق قبل T605–T611. ما أُنجز هنا هو أن المخطط صار
**أثراً مراجَعاً له بوابة** بدل أن يُحسب عند كل طلب — وهو الشرط الذي يجعل
تثبيته لاحقاً تبديل سطر واحد في `swagger_parser.yaml` وإعادة توليد.

---

## بوابة إغلاق الفيز

- [ ] كل التاسكات ✅
- [ ] G1–G6 مثبتة
- [ ] كل نقطة لها اختبار نجاح واختبار أهم فشل
- [ ] مراجعة جدول الأمان في `spec.md` بنداً بنداً
- [ ] المخطط مثبَّت ومولّد العميل يعمل

---

## إضافات لاحقة إلى العقد

ثلاث ثغرات كشفها بناءُ الويب في الفيز 011، وكلٌّ منها **ثغرةُ عقدٍ لا نقصُ
قناة**: الفيز 008 كانت ستصطدم بالثلاثة نفسها. فأُضيفت هنا حيث يرثها التطبيق
(القاعدة الأولى في الفيز 011: *ما يحتاجه الويب ولا يوفّره العقد يُضاف إلى العقد
ويستفيد منه التطبيق، أو لا يُضاف*).

### ✅ T622 — التسليم إلى بوابة الدفع
`GET /api/v1/wallet/topups/{reference}/checkout/`، و`checkout_url` على
`PaymentIntent`، و`apps/money/gateway.py`. 15 اختباراً.

**العنوان على خادمنا لا على البوابة.** كل بوابة تريد شيئاً مختلفاً — تحويلاً، أو
نموذج POST بحقول موقَّعة، أو رمز جلسة — وحقلٌ يحمل «عنوان البوابة» حقلٌ يتغيّر
معناه يوم تتغيّر البوابة، **وعلى القناتين أن تتغيّرا معه**. فالتبديل إعدادٌ في
`PAYMENT_CHECKOUT_TEMPLATE` وتعديلٌ في وحدةٍ واحدة، ولا يعرف أيٌّ من العميلين ما
«ميسر» أصلاً. وغير المُهيَّأ يرفض ولا يخمّن.

### ✅ T623 — المفضّلة
`GET/PUT/DELETE /api/v1/favourites/`، و`apps/auctions/favourites.py`.
16 اختباراً.

**العلامة صفٌّ لأن مفضّلة يراها العميل على الموقع ولا يجدها في التطبيق عيبٌ في
المنتج لا مزامنةٌ ناقصة.** وهي علامة لا ادّعاء: لا تحجز ولا تعطي أولوية ولا ترى
ما لا يُرى. والتكرار غير مؤذٍ في الاتجاهين، والفرادة قيدُ قاعدة.

### ✅ T624 — البثّ الحي
`GET /api/v1/live/` (SSE)، و`apps/bidding/live.py`. 14 اختباراً.

**وأهمّ ما فيه أنه لا يحمل رقم أحدٍ غيرك.** القراءة البديهية لـ«تحديث حي» شريطُ
أسعار، وهو ما لا تملكه هذه المنصّة عمداً: «لا نقطة تسرد المزايدات على مركبة —
خاصيّةُ المزاد المغلق كلّها أن المزايدين لا يرون أرقام بعضهم.» فالحيّ مزايدات
المتصل نفسه وحالاتٌ عامة، **وتغيّرٌ عند منافس لا يحرّك البصمة إطلاقاً** لأن
تحرّكها هو الإشعار.

بلا Channels ولا Redis: `StreamingHttpResponse` فوق ASGI القائمة، وبصمةٌ تُعاد
اشتقاقها كل نبضة بدل سجلّ أحداث يُلزم كل كاتبٍ بأن يُعلن عن نفسه.
