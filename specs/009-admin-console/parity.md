# قائمة التكافؤ — اللوحة الجديدة مقابل اللوحات الأربع القديمة

**الحالة: مسوّدة تنتظر المالك. غير موقَّعة.**
آخر جرد للّوحة الجديدة: 2026-09-03، من الشيفرة نفسها لا من وثيقة عنها.

**مراجعة ثانية للجرد — 2026-09-03.** أُعيد فحص كل صفّ في (ب) على الشيفرة، لا على
الجرد الأول. صُحِّح صفّان كان دليلهما خاطئاً (`attribute` و`deposit_insurance`
**لهما** مستدعٍ آلي)، وأُضيفت ستة صفوف لم يرها الجرد الأول، وأربع حقائق عن الدخول
شُوهدت بالتنفيذ لا استُنتجت — انظر الملحق (ز) في آخر الملف. القاعدة التي
تفرض هذه المراجعة واحدة: **قائمةٌ يُوقَّع عليها لا تحتمل خانةً «مؤكَّدة» وهي
مستنتَجة**، وسطرٌ خاطئ في وثيقةِ تكافؤ أخطر من سطر ناقص — الناقص يُسأل عنه،
والخاطئ يُصدَّق ويُبنى عليه قرار إطفاء.

> **⚠️ لا تُطفأ أي لوحة قديمة قبل توقيع المالك على هذه القائمة (I7، وQ4 في
> [الخارطة](../000-roadmap.md)).** الإطفاء قبل التوقيع ليس قراراً تقنياً يُتخذ
> نيابةً عنه: المالك يستعمل `admin2` يومياً، وما يعرفه عن استعمالها ليس مكتوباً
> في أي مكان في هذا المستودع.

---

## كيف تُقرأ هذه القائمة

صفٌّ واحد لكل **قدرة** — لا لكل شاشة. الشاشة تحمل عدة قدرات (تبحث، وترشّح،
وتصدّر، وتكتب)، وقائمةٌ بالشاشات تخفي بالضبط ما يجب أن يظهر: قدرةٌ كانت في
القديم داخل شاشةٍ لها مقابل هنا، ولا مقابل لها هي.

| العمود | ما يعنيه |
|---|---|
| **القدرة** | ما يستطيع الموظف فعله، بصيغة الفعل |
| **أين كانت** | في القديم — وله ثلاث قيم فقط، انظر أدناه |
| **أين صارت** | الشاشة والمسار في الجديدة، أو «—» |
| **الحالة** | ✅ / ⚠️ / ⛔ / ❓ |

**العمود «أين كانت» لا يُملأ بالتخمين.** له ثلاث قيم، وكلٌّ منها يقول من أين
جاء:

* **`v1 ‹مرجع›`** — سلوكٌ موثَّق في هذا المستودع عن النسخة الأولى، بمرجعه.
  هذه ليست ذاكرة أحد: كلها مكتوبة في specs أو في تعليقات الشيفرة، ومصدر كل
  واحدة منها حادثة حقيقية.
* **`الخلفية ‹دالة›`** — لا نصّ عن القديم، لكن الخلفية الجديدة تحمل الدالة،
  فالقدرة **مطلوبة** في مكان ما. وجودُ الدالة لا يقول إن اللوحة القديمة كانت
  تفعلها؛ يقول إن أحداً بنى لها طريقاً.
* **`؟`** — لا مصدر. **يملؤه المالك، ولا يُخمَّن.** خانةٌ فارغة تُسأل،
  وتخمينٌ في قائمةٍ يُوقَّع عليها يُصدَّق.

> **ولا مصدر واحد في المستودع يقول أيّ لوحة من الأربع.** كل ما هو موثَّق
> يقول «v1» أو «شاشة في v1». التفرقة بين `admin` و`admin2` و`admin3`
> و`admin_v2` — ومن يفتح أيّها اليوم — عند المالك وحده، وهي نصف السؤال:
> **قدرةٌ موجودة في لوحة لا يفتحها أحد لا يجب أن تُبنى مرة أخرى.**

| الرمز | معناه |
|---|---|
| ✅ | **موجودة** في اللوحة الجديدة، ومختبَرة |
| ⚠️ | **ناقصة** — لا شاشة لها في الجديدة |
| ⛔ | **مقصود إسقاطها** — بسبب مكتوب في العمود نفسه |
| ❓ | **لا يُعرف** إن كانت مطلوبة أصلاً — سؤال مفتوح في القسم (د) |

---

## 0 — ما يعرفه هذا المستودع عن اللوحات الأربع

المصدر الوحيد الذي يسمّيها الأربع هو
[`ops/checks/console_urls_are_named.py`](../../ops/checks/console_urls_are_named.py):
`admin` و`admin2` و`admin3` و`admin_v2`، وأن اللوحة انتقلت بين البادئات ثلاث
مرات فكُسر في كل مرة كل رابط مكتوب بيده — **بصمت**.

وهذا كل شيء. لا شيفرة لأيٍّ منها في المستودع، ولا لقطة شاشة، ولا وثيقة، ولا
قائمة مسارات. ما تبقّى مما نعرفه عن v1 متناثر في specs كحوادث، وهو ما يملأ
عمود «أين كانت» أدناه حيثما امتلأ.

**وهناك بابٌ خامس قائم اليوم لا يُذكر مع الأربعة:** لوحة Django على `/admin/`
مركّبة في [`config/urls.py`](../../backend/config/urls.py). كل ملفات
`apps/*/admin.py` فارغة، فلا تعرض نموذجاً واحداً من نماذجنا — لكنها **صفحة
الدخول الوحيدة في المشروع** (انظر الصفّ الأخير في القسم ب). أي قرار بإطفاء
«اللوحات القديمة» يجب أن يقول ماذا يحدث لها.

---

## أ — قدرات اللوحة الجديدة، قدرةً قدرة

مجرودة من [`apps/console/urls.py`](../../backend/apps/console/urls.py)
و[`navigation.py`](../../backend/apps/console/navigation.py) وملفات العرض
والقوالب — لا من وصفٍ مكتوب عنها. `navigation.PAGES` هو السجلّ الذي يحرس
الصفحات ويعرضها معاً، فلا يذكر شاشةً غير موجودة ولا ينسى شاشةً موجودة.

### التشغيل اليومي — المزادات والمركبات

| القدرة | أين كانت | أين صارت | الحالة |
|---|---|---|---|
| أرى ما أستطيع فتحه أنا حين أدخل | `v1` كانت الرئيسية لوحة أرقام لا يُفعل بها شيء، فبَصَم الجميع صفحةً فرعية ‹`console/views.py`› | الرئيسية — `console:home` | ✅ |
| أستعرض كل المزادات بحالاتها وعدد مركبات كلٍّ | ؟ | المزادات — `console:auctions` | ✅ |
| أبحث عن مزاد برقمه أو بعنوانه | ؟ | المزادات — `?q=` | ✅ |
| أُرشّح المزادات بالحالة | ؟ | المزادات — `?state=` | ✅ |
| أُنشئ مزاداً جديداً | ؟ | `console:auction-new` | ✅ |
| أعدّل مزاداً (الرقم، العنوان، التوقيت، التأمين المطلوب) | ؟ | `console:auction-edit` | ✅ |
| أرى مركبات مزادٍ واحد، وما ينتظر قراراً في أوّلها | `v1` كانت تجلس بترتيب اللوت في الصفحة الرابعة حتى يبحث عنها أحد ‹009/T805› | `console:auction-detail` — الترتيب مُختبَر كترتيب | ✅ |
| أبحث عن مركبة عبر المزادات (ماركة، طراز، لوت، رقم مزاد) | ؟ | المركبات — `console:vehicles?q=` | ✅ |
| أُرشّح المركبات بالحالة | ؟ | المركبات — `?state=` | ✅ |
| أُنشئ مركبة وأعدّل بياناتها (هيكل، لوحة، ممشى، ناقل، وقود، حالة فنية، شريك، سعر وقوف) | `v1` أربع شاشات تحسب سعر الوقوف بأربع طرق ‹005/T406›؛ وأربعة مسارات رسم وثلاث قوائم حقول تُخفي حقولاً بصمت ‹005/T413› | `console:vehicle-new` · `console:vehicle-edit` — و`reserve_price` حقل واحد | ✅ |
| أرى حالة مركبة والانتقالات المسموحة لها | `v1` شاشات مليئة بأزرار لانتقالات ترفضها الآلة ‹009/T805› | `console:vehicle-detail` — الانتقالات محسوبة من آلة الحالات | ✅ |
| أنقل مركبة من حالة إلى حالة بسبب مكتوب | `v1` طريق ثانٍ لتحريك مركبة جعل مركبة `awarded` بلا فائز ‹009/T805› | `console:vehicle-state` — POST فقط، والسبب إلزامي، ويُسجَّل | ✅ |
| أرفع ملف مركبات وأشغّله تجريبياً قبل الاعتماد | ؟ | `console:vehicles-import` — تشغيل تجريبي يتراجع فعلاً لا يتخطّى الكتابة | ✅ |
| أنزّل كل صفّ رُفض في الاستيراد باسمه وسببه | `v1` كان يتوقّف بعد خمسة ويقول «و95 غيرها» ‹009/T806› | `console:vehicles-import-errors` — ملف يعمل عليه المشغّل | ✅ |
| أصدّر المركبات في ملف يُرفَع مرة أخرى بلا تعديل | ؟ | `console:vehicles-export` — نفس كاتب الفيز 005 | ✅ |

### الشركاء والعروض

| القدرة | أين كانت | أين صارت | الحالة |
|---|---|---|---|
| أرى ما ينتظر قرار شريك، الأطول انتظاراً أولاً | `v1` **لا نظير لها**: المركبة تحت سعر الوقوف تجلس في القائمة بترتيب اللوت ويُبلَّغ الشريك بالهاتف ‹009/T807› | `console:partner-decisions` | ✅ |
| أُرشّح قرارات الشركاء بشريك بعينه | ؟ | `console:partner-decisions?partner=` | ✅ |
| أرى **كل** المزايدين على مركبة لا الأعلى وحده | `v1` استعلام قاعدة بيانات ‹009/T807› | `console:partner-offers` | ✅ |
| أرى العرض **المقبول** لا الأعلى | `v1` كانت تحسب أعلى مزايدة عند كل رسم، فمركبةٌ رست على الثاني تعرض رقم الأول — ودخل ذلك الرقم محادثة الفاتورة ‹009/T807› | `console:partner-offers` — يُقرأ من الترسية | ✅ |
| أُرسي على أيّ مزايد بنقرة | `v1` **لم يكن يستطيع**: المشغّل يلغي المزاد ويعيد العرض فيخسر كل مزايد مكانه ‹009/T807› | `console:partner-award` | ✅ |
| أنقل الترسية إلى مزايد آخر، بالفاتورة والتأمين معاً | `v1` يدوياً في أربع شاشات ‹006/T510› | `console:partner-award` — `replace_winner` في معاملة واحدة | ✅ |
| أسجّل رفض المالك بوصفه **رفضاً** لا سحباً | `v1` فقد التفرقة، وللحالتين خطوتان تاليتان متعاكستان ‹009/T807› | `console:partner-reject` | ✅ |

### المستخدمون والشركات

| القدرة | أين كانت | أين صارت | الحالة |
|---|---|---|---|
| أبحث عن عميل بالجوال أو الاسم أو رقم الهوية | `v1` اختلف الاسم المعروض بين الشاشات فلم يعرف الدعم من زايد ‹001/T012› | `console:customers?q=` | ✅ |
| أرى ملفّ عميل: بياناته، وفلوسه، وآخر فواتيره | ؟ | `console:customer-detail` | ✅ |
| أصحّح بيانات عميل بلا أن يُجهض خطأٌ في حقل باقي الحقول | `v1` تحت `STRICT_TRANS_TABLES` يُجهض التحديث كله، والرسالة تسمّي الجملة لا الصندوق ‹009/T808› | `console:customer-edit` | ✅ |
| أصحّح بيانات شركة (سجل، ضريبي، عنوان زاتكا) | ؟ | `console:company-edit` | ✅ |
| أُثبّت رقم هوية صحيحاً ولا يُستبدل بعدها | `v1` عمود الهوية حمل رقمَي شخصين ‹009/T808› | `console:customer-edit` — نفس قاعدة شاشة العميل | ✅ |

### المال

| القدرة | أين كانت | أين صارت | الحالة |
|---|---|---|---|
| أستعرض الفواتير وأرشّحها بالحالة وأبحث برقمها أو بصاحبها | ؟ | `console:invoices` | ✅ |
| أرى حالة فاتورة **مشتقّة من دفعاتها المرحَّلة** | `v1` كان يعكس حالة أودو في عمود يُكتب مرة عند الإدراج، فحوالةٌ مسودّة ظهرت مسدَّدة **وأُفرج عن مركبة بها** ‹009/T809 · 002/T116› | `console:invoice-detail` — `derive_invoice_state` | ✅ |
| أرى كلام أودو بجوار حالتنا موسوماً بأنه كلامهم | `v1` كان ينسخه بوصفه حقيقة ‹المادة ٢-٣› | `console:invoice-detail` | ✅ |
| أرى من يملك رصيداً عندنا ومجموعه وحجوزاته | ؟ | `console:money-ledger` | ✅ |
| أفتح دفتر عميل من رقم جواله مباشرةً | ؟ | `console:money-ledger?q=` — رقم كامل يُعيد التوجيه | ✅ |
| أرى دلاء عميل وحجوزاته بأسبابها وكل حركاته | ؟ | `console:money-customer` — **قراءة فقط بالبناء** | ✅ |
| أُنبَّه حين يختلف الرصيد المخزَّن عن قيوده | ؟ | `console:money-customer` — تحذير فوق الرقم لا رقم صامت | ✅ |
| أصادر حجزاً بسبب مكتوب وباسمي | `v1` مصادرةٌ وتعديلٌ يدوي لا يُفرَّقان بعدها عن عطل، إذ لا جملة تقول من قرّر ولماذا ‹009/T811› | `console:money-confiscate` | ✅ |
| أصحّح حركة بقيد عاكس يشير إلى ما يعكسه | `v1` التصحيح تعديل ‹المادة ١-٤› | `console:money-correct` | ✅ |
| أمنح استثناء مزايدة لمدين، بثقة أعلى من المصادرة | `v1` كان يُمنح بتعديل عمودين يدوياً بلا أثر، والثلاثة خلف علم «مالية» واحد ‹006/T515 · 009/T811› | `console:money-exception` — `money.exception` فوق `money.act` | ✅ |

### التشخيص

| القدرة | أين كانت | أين صارت | الحالة |
|---|---|---|---|
| أسأل «ليه ما يقدرش يزايد؟» برقم جوال وأجد الجواب في طلب واحد | `v1` بلا جواب مخزَّن: كل حالة تُعاد بناؤها يدوياً من عدة جداول **بعد** أن تحرّكت الأرصدة ‹006/T503 · 009/T812› | `console:why-no-bid?phone=` — مع لقطة فلوسه وقت الرفض | ✅ |
| أرى اختلاف الدفتر مع نفسه | ؟ | `console:money-health` — `verify_ledger` | ✅ |
| أرى فروقنا مع أودو | ؟ | `console:money-health` — `open_differences` | ✅ |
| أرى المعلّق بلا صاحب | ؟ | `console:money-health` — رصيد الدلو نفسه لا مجموع ما وصله | ✅ |
| تُغلق الملاحظة بزوال سببها بلا أن يشطبها أحد | `v1` طابور المطابقة كان أقدمُ بنوده كلها أشياء أُصلحت قبل شهور ولم يشطبها أحد، فتُرك الطابور كله ‹009/T813› | `console:money-health` — تُحسب عند كل فتح، ولا جدول ملاحظات أصلاً | ✅ |
| أرى كل رسالة واردة من أودو بحالتها ونصّها كما وصل | `v1` حجبت فاتورتان مخترَعتان من بيئة الاختبار مزايداً حقيقياً 3.5 ساعة ‹003/T204› | `console:odoo-inbox` · `console:odoo-message` | ✅ |
| أُرشّح الوارد بالحالة والمصدر والموضوع | ؟ | `console:odoo-inbox` — و«الفاشلة» بنقرة، وليست العرض الافتراضي | ✅ |
| أُعيد تشغيل رسالة عبر **نفس** المسار الآلي | ؟ | `console:odoo-replay` — `process()` نفسها التي يستدعيها الكرون، ومُثبَت بالمقارنة | ✅ |

### الإدارة والسلوك العام

| القدرة | أين كانت | أين صارت | الحالة |
|---|---|---|---|
| أبحث في سجل التدقيق بالمنفّذ أو الكيان أو الفترة أو الفعل | ؟ | `console:audit` — وقائمة الأفعال مشتقّة من الصفوف لا مكتوبة | ✅ |
| كل صفحة محروسة بالصلاحية نفسها التي تعرضها في القائمة | `v1` قائمة تُرندَر وقائمة أخرى تُعدَّل، فظهرت صفحات جديدة في الخطأ منهما — و`hasRole()` المتساهلة مع المالك **أقفلت اللوحة في وجهه** ‹009/T801-T802› | `navigation.PAGES` — صفٌّ واحد، واختبار يقارن القائمة بالمسموح | ✅ |
| كل قائمة تُصدَّر إلى `.xlsx` حقيقي **يحترم المرشِّح** | `v1` أربع شاشات بأربعة تصديرات، وكلٌّ يصدّر الجدول كله فنسخ الناس الصفوف بأيديهم ‹009/T817› | `?export=xlsx` على تسع قوائم — انظر التحفّظ (١) أدناه | ✅ |
| كل كتابة تترك: من فعل، وما كان قبلها، ولماذا | ؟ | `apps/core/audit` على كل مسار كتابة في اللوحة | ✅ |
| كل صفحة تقول أي بيئة هي | ؟ | `context.navigation` — لا يُنسى في شاشة ‹المادة ٥-٦› | ✅ |
| كل رابط يحترم بادئة اللوحة | `v1` انتقلت البادئة ثلاث مرات فكُسر كل رابط مكتوب بيده، بصمت ‹T804› | `{% url %}` وفحص يرفض المسار المكتوب | ✅ |

### تحفّظان على «✅» أعلاه

الصفّ الأخضر يقول إن القدرة موجودة، لا إنها موجودة **لكل من كان يستعملها**. وهذان
تحفّظان يقرأهما الموقِّع قبل أن يقرأ ✅ على أنها تكافؤ تام:

**(١) تسع قوائم تُصدَّر، لا كل شاشة.** التصدير على: المزادات · المركبات · سجل
التدقيق · وارد أودو · دفتر التأمينات · دفتر عميل · قرارات الشركاء · المستخدمون ·
الفواتير. **وليس على** `console:money-health` ولا `console:partner-offers` —
صفوف الفروق، وقائمة المزايدين على مركبة. لا يقول هذا إنهما ناقصتان: يقول إن ✅
التصدير لا تشملهما، ودرس v1 المكتوب في `exports.py` أن ما لا يُصدَّر يُنسَخ
بالعين. **هل يُنسَخ منهما اليوم؟ س23 — والمالك وحده يعرف.**

**(٢) بعض ✅ لا يراها من كان يراها.** الصلاحية تُقاس بالدور، وقائمة الأدوار
مكتوبة في `apps/core/permissions.py`. المالك يملك كل شيء، فالقائمة كلها ✅ في
وجهه هو؛ ولمن سواه ثلاثة أبواب مغلقة تستحق أن تُقال بصريح العبارة:

| الشاشة | خلف | ليست عند |
|---|---|---|
| العروض والترسية ورفض المالك | `partners.decide` | المالية · الدعم |
| دفتر التأمينات ودفتر عميل | `money.view` | **التشغيل** |
| سجل التدقيق · وارد أودو | `audit.view` · `odoo.inbox` | التشغيل · الدعم |

فمن كان في القديم يفتح شاشةً واحدة يرى فيها كل شيء قد يجد بابه مغلقاً هنا وهو
«موجود» في هذه القائمة. هذا مقصود (T801) — الأدوار الأربعة صُمِّمت لتفصل
التشغيل عن المال — لكنه **تغييرٌ في من يفعل ماذا**، والمالك يوقّع على ذلك أيضاً
لا على وجود الشاشة وحده. **من يشغّل هذه الأدوار الأربعة اليوم؟ س22.**

---

## ب — قدرات لا شاشة لها في الجديدة

هذا هو القسم الذي أنتجه الجرد ولم يكن في المسوّدة السابقة. كل صفّ هنا **قدرة
بنى لها أحدٌ طريقاً في الخلفية** (دالة خدمة، أو نموذج، أو صلاحية) **ولا يبلغها
موظف من اللوحة**. وجودها في الخلفية دليلٌ على أنها مطلوبة؛ وليس دليلاً على أن
اللوحة القديمة كانت تفعلها — ذلك ما يقوله المالك.

| القدرة | الدليل أنها مطلوبة | أين صارت | الحالة |
|---|---|---|---|
| أُلغي مزاداً | `bidding/settlement.py:cancel_auction` — **بلا مستدعٍ واحد في الإنتاج**، ومكتوب في آلة الحالات أنها تفكّ كل حجز وتُبطل الترسيات | — | ⚠️ |
| أجدول مزاداً أو أُنشّطه أو أُنهيه أو أُسوّيه بيدي | `auctions/services.py`: `schedule` · `activate` · `end` · `settle`؛ يستدعيها الكرون وحده، ولا زرّ في أي شاشة. و`AUCTION_MOVES` في `auctions/states.py` تُعرِّف ثماني نقلات للمزاد — **لا واحدة منها لها زرّ**، بينما نقلات المركبة التسع عشرة كلها لها زرّ في `console:vehicle-state`، محسوبةً من الآلة نفسها. **نموذج تحرير المزاد يُسقط حقل الحالة عمداً** (`AuctionForm.Meta.fields`) — لكن البديل المقصود (زرّ نقلة كما في المركبة) لم يُبنَ | — | ⚠️ |
| أعيد عرض مركبة في مزاد لاحق | `bidding/settlement.py:relist_vehicle` — بلا مستدعٍ. وهي الخطوة التالية المكتوبة لـ«رفض المالك» | — | ⚠️ |
| أنسب مبلغاً معلّقاً إلى صاحبه **بيدي** | `money/services.py:attribute` — **له مستدعٍ واحد** هو `credit_payment`، أي المسار الآلي حين يصل من أودو ما يسمّي صاحب الدفعة لاحقاً. فأما مبلغٌ لم يصل عنه ذلك أبداً فلا شيء ينسبه؛ وشاشة صحة المال **تعرضه** ولا تعطي طريقاً لحلّه | — | ⚠️ |
| أسجّل تأميناً وصل خارج البوابة (حوالة بنكية) | `money/services.py:deposit_insurance` — **له مستدعيان**، كلاهما آلي: `credit_payment` و`odoo/processing.py`. لا مستدعٍ يبدأ من إنسان، ولا شاشة | — | ⚠️ |
| أرى طلبات الاسترداد وأعتمدها أو أرفضها | `money/services.py:request_refund` من API العميل فقط؛ و`refund_insurance` لا يحرّكها إلا رسالة أودو مؤكَّدة. وv1 كان له جدول `refunds_requests` ‹004/spec› | — | ⚠️ |
| أُصدر فاتورة أو أسجّل دفعة يدوياً | **`invoices.manage` صلاحية معرَّفة، ممنوحة للمالك والمالية، ولا تحرس صفحة واحدة** — لا صفّ لها في `navigation.PAGES` | — | ⚠️ |
| أسحب مزايدة نيابةً عن عميل | `bidding/services.py:withdraw_bid` — من API العميل فقط | — | ⚠️ |
| أرفع صور مركبة | `auctions/services.py:add_image` — بلا مستدعٍ، ولا حقل صور في `VehicleForm` | — | ⚠️ |
| أمنح موظفاً صلاحية أو أسحبها، أو أغيّر دوره | `accounts/models.py:StaffGrant` و`User.console_role` — النموذج والهجرة موجودان، **ولا شاشة ولا تسجيل في Django admin**. التغيير اليوم لا يقع إلا بيد على قاعدة البيانات | — | ⚠️ |
| أراقب الصادر إلى أودو (ما لم يُرسَل، وما أُعيدت محاولته) | `odoo/outbox.py` — طابور وعامل إرسال، وله صندوق وارد بشاشتين ولا شيء للصادر | — | ⚠️ |
| أرى ما أُرسل للعميل من رسائل وإشعارات وماذا حلّ بها | `notifications/models.py`: `Notification` · `Device` · `DeliveryState` — بلا شاشة. و«تعذّر إرسال رمز التحقق» في v1 كان يعني نفاد رصيد الرسائل ‹007/T603› | — | ⚠️ |
| أستعرض الشركاء/الشركات كقائمة | لا شاشة قائمة للشركات؛ الشركة تُحرَّر من صفحة عميلها وحده، ومرشِّح الشركاء في `partner-decisions` يأخذ رقم شركة يُكتب باليد | — | ⚠️ |
| **أدخل إلى اللوحة** | `LOGIN_URL` غير مضبوطة، فقيمتها الافتراضية `/accounts/login/` — **ومسارٌ بهذا الاسم غير موجود**. كل صفحات اللوحة `login_required`، فالموظف غير المسجَّل يُحوَّل إلى صفحة **404**، لا إلى صفحة دخول. الدخول الوحيد الممكن أن يعرف الموظفُ بنفسه أن يكتب `/admin/login/` | `/admin/login/` — خارج اللوحة، ولا شيء يدلّ عليه | ⚠️ |
| أخرج من اللوحة | لا رابط خروج في `base.html` ولا مسار | — | ⚠️ |
| أُنشئ حساب موظف للّوحة | `manage.py createsuperuser` أو يدٌ على قاعدة البيانات. لوحة Django **لا تسجّل نموذج المستخدم** — المسجَّل فيها `auth.Group` وحده — فحتى الباب الخامس لا يفتح هذا | — | ⚠️ |
| أُنشئ حساب عميل يدوياً | `accounts/services.py:user_for_verified_phone` هي الطريق الوحيد، وتشترط جوالاً مُثبَتاً برمز. لا شاشة إنشاء عميل في اللوحة. **قد يكون ذلك مقصوداً** — لكن لا قرار مكتوب به في المستودع، فهو سؤال لا إسقاط (س17) | — | ⚠️ |
| أوقف عميلاً عن الدخول أو أُعيد تفعيله | `accounts/models.py:User.is_active` حقلٌ قائم، **وليس من حقول `CustomerForm`** ولا لأي شاشة طريق إليه. إيقاف مزايد اليوم يقع على قاعدة البيانات | — | ⚠️ |
| أرى محاولات الدفع عبر البوابة وحالتها | `money/models.py`: `PaymentIntent` — صفٌّ يُكتب **قبل** أن يصل العميل البوابة، بستّ حالات فيها `failed` و`expired` و`disputed`. **لا شاشة في اللوحة تقرؤه.** فإن سأل عميلٌ «دفعت ولم يصل» فالمحاولة الفاشلة مكتوبة عندنا ولا يبلغها موظف — هل يُسأل عنها اليوم؟ س19 | — | ⚠️ |
| أرى مزايدات عميل بعينه — **المقبولة** لا المرفوضة وحدها | `console:why-no-bid` يعرض `BidRefusal` فقط، و`console:partner-offers` يعرض مزايدي **مركبة واحدة**. لا شاشة تسأل «ماذا زايد هذا الشخص؟» | — | ⚠️ |
| أرى الحجوزات القائمة في مزاد بعينه | `money/models.py:Hold.auction` مفتاحٌ قائم، و`console:money-ledger` يعدّ الحجوزات **لكل عميل** ولا يرشّحها بمزاد. سؤال التسوية «من عليه حجز في مزاد اليوم؟» بلا شاشة | — | ⚠️ |
| تقارير دورية (مبيعات، تحصيل، أداء مزاد) | لا شيء في المستودع. التصدير يعطي القوائم خاماً | — | ❓ |

> **لا يعني ⚠️ أن الفيز 009 ناقص.** يعني أن هذه القدرة ليست في اللوحة، فإن كان
> المالك يفعلها اليوم في القديم فهي **شرطٌ في الإطفاء** لا بند تحسين لاحق. وإن
> كان لا يفعلها، فهي دالةٌ في الخلفية بلا مستهلك — وذلك بذاته اكتشاف.

---

## ج — ما أُسقط عمداً، وبسبب مكتوب

هذه ليست ثغرات. كلٌّ منها **كان ممكناً في v1** أو هو الشكل المعتاد في لوحات
الإدارة، ومُنع هنا بقرار. المالك لا يُطلب منه أن يوافق على السبب — يُطلب منه
أن يقول إن كان يعتمد على أيٍّ منها اليوم.

| ما أُسقط | لماذا | البديل في الجديدة |
|---|---|---|
| تعديل رصيد عميل مباشرةً | لا يوجد «تعديل رصيد» أصلاً؛ كل حركة معاملة متوازنة ‹المادة ١-١› | مصادرة أو تصحيح، كلٌّ بسببه واسم منفّذه |
| حذف أو تعديل معاملة مُرحَّلة | التاريخ يجب أن يبقى قابلاً لإعادة البناء عند النزاع ‹المادة ١-٤› | قيد عاكس يشير إلى ما يعكسه |
| تغيير حالة مركبة من نموذج التحرير | حقل `state` في نموذج طريقٌ ثانٍ للتحريك، وهو ما جعل مركبة في v1 `awarded` بلا فائز ‹009/T805› | `console:vehicle-state` — نقلة واحدة بسبب |
| كتابة الترسية أو سعرها بيد | ترسيةٌ بلا مزايدة خلفها ولا فلوس تحرّكت لها ‹009/T805› | `award_to` / `replace_winner` من شاشة العروض |
| تحرير رسالة أودو أو ضبط حالتها بيد | تعديل الرسالة يحوّل الدليل إلى تخمين، و«معالَجة» بلا معاملة خلفها دفعةٌ موجودة عندهم ومفقودة عندنا ‹009/T814 · المادة ٢-٢› | إعادة تشغيل عبر المسار الآلي نفسه |
| ترشيح الفواتير بحالة أودو | شاشةٌ تجيب «ماذا يقول أودو» والسؤال «ما الذي لنا» ‹009/T809› | الترشيح بالحالة المشتقّة؛ وكلام أودو معروض كدليل |
| أزرار أفعال داخل دفتر التأمينات | الدفتر هو الشاشة التي تُفتح والعميل على الهاتف، وقيمتها من عجزها عن تغيير شيء — واختبارٌ يفشل إن ظهر فيها `<form method="post">` ‹009/T810-T811› | صفحة «أفعال مالية» يُذهَب إليها قصداً |
| تعديل أو حذف صفّ تدقيق | سجلٌّ يمكن تحريره لا يثبت شيئاً | لا شيء — `AuditLog.save` يرفض الكتابة الثانية |
| تغيير جوال العميل من شاشة الموظف | مسار استيلاء حقيقي في v1؛ يحتاج تأكيد الرقمين ‹007/T604› | لا شيء في اللوحة — المسار للعميل نفسه |
| منح صلاحية داخل شاشة تعديل العميل | تغيير وصولٍ مدفون في تعديل ملفّ تغييرٌ لا يراجعه أحد ‹009/T808› | يحتاج شاشته الخاصة — **ولم تُبنَ** (القسم ب) |
| لوحة أرقام في الصفحة الرئيسية | أرقامٌ لا يُفعل بها شيء، فبَصَم الجميع صفحةً فرعية ‹`console/views.py`› | الرئيسية = ما تستطيع أنت فتحه |

---

## د — الأسئلة المفتوحة، بأسمائها ومن يجيبها

لا يُوقَّع على هذه القائمة قبل أن يكون لكل صفّ هنا جواب مكتوب.

| # | السؤال | لماذا يهم | من يجيبه |
|---|---|---|---|
| س1 | أيّ من الأربع تُفتح فعلاً اليوم، وكم مرة، ومن يفتحها؟ | قدرةٌ في لوحة لا يفتحها أحد لا تُبنى ثانيةً. وحتى `admin2` لا يُعرف عنها إلا أن المالك يفتحها يومياً | المالك |
| س2 | ماذا يفعل المالك في `admin2` في يوم عادي — بالترتيب؟ | هذا هو نصف القائمة الذي لا يملكه المستودع، وهو الفرق بين «ما تستطيع الشاشة» و«ما يُفعل» | المالك |
| س3 | من يرفع صور المركبات اليوم، وفي أي لوحة؟ | `add_image` مبنيّة بلا مستهلك؛ ومزادٌ بلا صور ليس مزاداً | المالك / التشغيل |
| س4 | من يعتمد طلبات الاسترداد اليوم، وأين، وبأي دليل؟ | لا شاشة في الجديدة، وv1 له جدول `refunds_requests` | المالك / المالية |
| س5 | هل تُصدَر فاتورة أو تُسجَّل دفعة يدوياً اليوم؟ | `invoices.manage` صلاحية بلا صفحة — إما تُبنى الصفحة أو تُحذف الصلاحية | المالية |
| س6 | كيف يُسجَّل تأمين يصل بحوالة بنكية خارج البوابة؟ | `deposit_insurance` لا يستدعيها إلا المسار الآلي؛ لا شاشة ولا مستدعٍ يبدأ من إنسان | المالية |
| س7 | من ينسب المبالغ المعلّقة لأصحابها اليوم؟ | الشاشة تعرض المعلّق ولا تحلّه؛ و`attribute` لا تُستدعى إلا حين يسمّي أودو صاحب الدفعة لاحقاً — فمبلغٌ لم يصل عنه ذلك يبقى معلّقاً بلا طريق | المالية |
| س8 | من يمنح موظفاً صلاحية اليوم، وأين؟ | `StaffGrant` بلا شاشة — التغيير اليوم بيد على قاعدة البيانات | المالك |
| س9 | هل يُلغى مزاد أو يُنهى يدوياً اليوم؟ وكم مرة في الشهر؟ | `cancel_auction` بلا مستدعٍ، والجدولة كلها بالكرون | التشغيل |
| س10 | هل يُسحب مزايدة نيابةً عن عميل؟ | مبنيّة للعميل وحده | الدعم |
| س11 | هل يُراقَب الصادر إلى أودو اليوم من شاشة؟ | صندوق الوارد مبنيّ والصادر لا | المالية |
| س12 | ما التقارير التي تُطبع أو تُصدَّر دورياً، ولمن؟ | لا تقارير في الجديدة، والتصدير يعطي القوائم خاماً | المالك |
| س13 | هل تُرسَل رسائل أو إشعارات يدوياً من لوحة اليوم؟ | `Notification`/`Device` بلا شاشة | المالك / الدعم |
| س14 | ما مصير لوحة Django على `/admin/` بعد التحويل؟ | هي صفحة الدخول الوحيدة اليوم؛ «لوحة واحدة» لا تكتمل بها مفتوحة ولا بلا بديل للدخول | المالك + الفريق |
| س15 | هل توجد قدرة في القديم لا يظهر لها أثر في هذا المستودع أصلاً؟ | القسم (هـ) وُجد لها | المالك |
| س16 | كيف يدخل الموظف اللوحة القديمة اليوم — صفحة دخول بكلمة سرّ؟ برقم جوال ورمز؟ | الجديدة **لا صفحة دخول لها**، والتحويل ينتهي إلى 404 (الملحق ز). ما يُبنى يجب أن يشبه ما يعرفونه، وإلا فيوم التحويل يوم لا يدخل فيه أحد | المالك + الفريق |
| س17 | هل يُنشأ حساب عميل من اللوحة اليوم — عميلٌ يأتي المعرض ولا يُنزّل التطبيق؟ | لا شاشة إنشاء، والطريق الوحيد جوالٌ مُثبَت برمز. إن كان يحدث فهو شرطٌ في الإطفاء | المالك / التشغيل |
| س18 | هل يُوقَف عميل عن الدخول أو المزايدة اليوم، وبأي إجراء؟ | `is_active` قائم بلا شاشة؛ والإيقاف اليوم يقع على قاعدة البيانات | المالك / الدعم |
| س19 | حين يقول عميل «دفعت ولم يصل»، أين يُنظر اليوم؟ | `PaymentIntent` بحالاته الستّ مكتوبٌ عندنا وبلا شاشة تقرؤه | الدعم / المالية |
| س20 | هل يُسأل عن «ماذا زايد فلان» في شاشة، أم يُبحث في المركبة مركبةً مركبة؟ | لا شاشة لمزايدات شخص؛ الموجود رفضاتُه ومزايدو مركبةٍ واحدة | الدعم |
| س21 | عند تسوية مزاد، كيف يُعرف من عليه حجز فيه؟ | `Hold.auction` موجود ولا يُرشَّح به؛ والعدّ في دفتر التأمينات لكل عميل لا لكل مزاد | المالية / التشغيل |
| س22 | من يشغّل كل دور من الثلاثة غير المالك (تشغيل · مالية · دعم)، ومن كان يفتح شاشة العروض والترسية ودفتر التأمينات في القديم؟ | الأدوار هنا تفصل التشغيل عن المال، وv1 جمعهما: علمٌ واحد للعرض والإدارة «فمن يقدر يشوف التاريخ يقدر يلغي لوت»، وعلم «مالية» واحد للقراءة والمصادرة ‹`core/permissions.py`›. فمن كان يرى كل شيء في شاشة واحدة قد يجد باباً مغلقاً وهو «موجود» في هذه القائمة (التحفّظ ٢) | المالك |
| س23 | هل يُنسخ من شاشتَي الفروق والمزايدين إلى ملف اليوم؟ | لا تصدير لهما؛ ودرس v1 المكتوب في `exports.py` أن ما لا يُصدَّر يُنسَخ بالعين (التحفّظ ١) | المالية / التشغيل |

---

## هـ — ما يفعله المالك في القديم ولا يجده أعلاه

*(يملؤه المالك. صفٌّ واحد لكل شيء يفعله ولا يجد له سطراً في (أ) ولا في (ب).)*

**الطريقة:** يُفتح `admin2` وتُمرّ شاشاتها شاشةً شاشة، ويُكتب في العمود الأول
ما **يُفعل** فيها فعلاً — لا ما تستطيع أن تفعله. ثم `admin3`، ثم `admin`
و`admin_v2` إن كانتا تُفتحان أصلاً (س1).

| ما يفعله | في أي لوحة | كم مرة | ما البديل المقترح |
|---|---|---|---|
| | | | |
| | | | |
| | | | |

---

## و — التوقيع

لا يُوقَّع على هذه القائمة وفيها صفٌّ ناقص. الشروط الأربعة:

- [ ] كل `؟` في عمود «أين كانت» صار له جواب أو شُطب الصفّ
- [ ] كل صفوف القسم (ب) قُرِّر فيها: تُبنى قبل الإطفاء، أم لا تُستعمل أصلاً
- [ ] كل أسئلة القسم (د) لها جواب مكتوب
- [ ] القسم (هـ) مُراجَع، وما فيه منقول إلى تاسكات بأسمائها

| | |
|---|---|
| راجع القائمة | |
| التاريخ | |
| البنود المتبقّية في (ب) | |
| البنود المتبقّية في (هـ) | |
| **مأذون بإطفاء `admin`** | ☐ نعم ☐ لا |
| **مأذون بإطفاء `admin2`** | ☐ نعم ☐ لا |
| **مأذون بإطفاء `admin3`** | ☐ نعم ☐ لا |
| **مأذون بإطفاء `admin_v2`** | ☐ نعم ☐ لا |
| **قرار لوحة Django `/admin/`** | ☐ تبقى ☐ تُقفل بعد بناء صفحة دخول |

> الإطفاء نفسه ليس فعلاً واحداً: [T921](../010-operations-cutover/tasks.md)
> يُطفئ v1 بالتدريج، وهذه القائمة شرطُ بدايته لا نهايته.

---

## ز — ما شُوهد بالتنفيذ

الجرد كله قراءةُ شيفرة، وقراءةُ الشيفرة تكفي لـ«لا مستدعٍ» ولا تكفي لـ«ماذا يحدث
حين يفتحها إنسان». هذه الأربع نُفِّذت فعلاً في `manage.py shell` على
`config.settings.dev` لأن أوّلها كان **خطأً في المسوّدة السابقة**: قيل فيها إن
الدخول «يمرّ بصفحة دخول لوحة Django»، والتنفيذ يقول غير ذلك.

وهي تسري على البيئات الثلاث لا على `dev` وحدها: `LOGIN_URL` **لا تُضبَط في أي
ملف تحت `config/settings/`**، و`config/urls.py` واحد للجميع.

| ما نُفِّذ | الناتج | ما يعنيه |
|---|---|---|
| `settings.LOGIN_URL` | `'/accounts/login/'` | غير مضبوطة، فهي قيمة Django الافتراضية |
| `resolve('/accounts/login/')` | **`Resolver404`** | الموظف غير المسجَّل يُحوَّل إلى مسار غير موجود. **لا يصل إلى صفحة دخول؛ يصل إلى 404** |
| `resolve('/admin/login/')` | `AdminSite.login` | صفحة الدخول الوحيدة القائمة — ولا رابط في اللوحة يدلّ عليها |
| `admin.site._registry` | `['auth.Group']` | لوحة Django لا تعرض نموذج المستخدم ولا `StaffGrant` ولا أي نموذج من نماذجنا. فمنح صلاحية أو إنشاء موظف **لا يقعان منها هي أيضاً** — يقعان على قاعدة البيانات مباشرةً |

> **ولهذا س14 ليست بنداً إدارياً.** «تبقى `/admin/` أم تُقفل» سؤالٌ عن الباب
> الوحيد الذي يُدخَل منه اليوم: إقفالها قبل بناء صفحة دخول يُقفل اللوحة الجديدة
> في وجه كل من فيها — وهي، حرفياً، شكلُ الحادثة التي وُجدت `permissions.py` لمنع
> تكرارها، بسبب آخر.
