# P5-02 · الإشعارات والأخبار

> `NotificationController` + `NewsTickerController`.
> `notifications` = **543,354 صفاً** · `news_ticker` = 3.

## الإشعارات
FCM — مشروع `haraj1-d4430`، موضوع `all_users`، الاعتمادات في `config/secrets/`.
الجدول ضخم: أي استعلام عليه يجب أن يكون مفهرساً ومحدود الصفحات، وتحتاج سياسة تقادم.

## الشريط الإخباري
مرسوم من الخادم من `news_ticker` (8 أعمدة)، ويُدار من `/admin_v2/news`.

## قواعد
1. **الرسائل النصية تُرسَل بالفعل من نسخة الاختبار** — الربط حيّ. لا ترسل تجارب.
2. 543 ألف صف بلا أرشفة = بطء متزايد؛ ضع سقفاً زمنياً.

## معايير القبول
- [ ] إرسال إشعار جماعي لا يحجب الطلب
- [ ] الشريط يُحدَّث بلا نشر كود
