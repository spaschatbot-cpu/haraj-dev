# قواعد العمل في هذا المستودع

هذه قواعد **كيف يُشتغَل**، لا ماذا يُبنى. ما يُبنى في
`.specify/memory/constitution.md` و`docs/system-handbook.md`، والدستور يعلو على
هذا الملف عند أي تعارض.

> **حُذفت المادة الرابعة** — «لكل إصلاحٍ حارسٌ، ولكل حارسٍ مخالفةٌ مصنوعة» —
> بقرار المالك في ٨ سبتمبر ٢٠٢٦، ومعها حزمةُ الاختبارات و`ops/checks` من
> المستودع (`ad916d2`). وأُعيد ترقيم ما بعدها.

## ١ — نفِّذ، ولا تستأذن في ما هو واضح

المالك طلب هذا بالحرف: «انجز ونفّذ التاسك بدون كلام كتير».

- التاسك المفتوح في `specs/*/tasks.md` **إذنٌ بتنفيذه**. لا يُسأل عنه مرة ثانية.
- السؤال يُطرح في حالة واحدة: قراءتان مختلفتان للطلب تُنتجان **عملاً مختلفاً**،
  ولا يحسم بينهما الكود ولا السجلّ. غير ذلك: اختر الأقرب، نفّذ، واذكر ما افترضت
  في سطر.
- «أبدأ؟» و«تحب أكمل؟» بعد جوابٍ سابق بنعم ليست أسئلة — إنما توقّفٌ بلا سبب.
- ما يُوقف العمل حقاً: حذفٌ أو كتابةٌ فوق شيء قائم، أو فعلٌ خارج الجهاز (دفعٌ
  إلى مستودع بعيد، إرسال، نشر)، أو تغييرُ إعداداتٍ ومفاتيح تكامل (المادة ٥-٤).

## ٢ — لا عملية بلا سبب

- **لا تُعِد قياس ما قِسته للتوّ.** إن كان الجواب في الجلسة فاستعمله.
- **افحص قبل أن تُصلح.** الأداة التي تكشف السبب أرخص من إصلاحٍ لسببٍ مُخمَّن —
  وقد كلّف تخمينُ «امسح الكاش» جلستين قبل أن يتبيّن أن الخادم يعمل بـ`--noreload`.
- **الأوامر المستقلّة تُرسَل معاً** في رسالة واحدة، لا واحداً في إثر واحد.

## ٣ — الردّ: النتيجة أولاً، والباقي عند الطلب

- ابدأ بما حدث. لا تمهيد، ولا سرد لما ستفعله، ولا إعادة لما فُعل.
- **الأرقام لا الصفات**: «٥٣ صفحةً في السجلّ» لا «اللوحة شبه كاملة»، و«٦٨ قدرة
  منها ٢٥ غائبة» لا «فيه نواقص».
- **قُل ما فشل ولماذا** بصراحة، ولا تُعلن إنجازاً لم تشاهده. وحدُ الأداة يُقال:
  «قرأتُ الكود ولم أفتح الشاشة» جملةٌ صحيحة ولازمة.
- لا تُكرّر ما في الرسالة السابقة، ولا تُذيّل بعرض خدماتٍ إضافية.

## ٤ — الرسائل والتعليقات تقول *لماذا*

- رسالة الـcommit بالعربية، تقول العطل والسبب، لا قائمة ملفات.
- التعليق يشرح القرار وثمنه، لا ما يقوله السطر. وحين يُوجد عطلٌ في الإنتاج أو
  في v1 فاذكره — فهو ما يمنع «تبسيط» القاعدة لاحقاً.

## ٥ — الحالة تُقال كما هي

- `🟡` حالةٌ شريفة: شيءٌ لم يُشاهَد، أو تبعيّةٌ لم تصل. **لا يُدَّعى ما لم
  يُشاهَد** — ورؤيةُ الشاشة تعمل قياسٌ، وقراءةُ الكود ليست قياساً.
- ما يحتاج جهازاً حقيقياً أو توقيع المالك يبقى مفتوحاً ومكتوباً في `tasks.md`.

## ٦ — البيئة

- PostgreSQL: الثنائيات في `C:\pg17\pgsql\bin` و**القاعدة في `C:\pg17\data`**
  (لا تحت `pgsql\`)، على المنفذ 5432. و**ليست خدمةً مسجَّلة**
  (`Get-Service postgresql*` يُرجع لا شيء) — تُشغَّل باليد وتموت مع إقلاع
  الجهاز:

  ```powershell
  & 'C:\pg17\pgsql\bin\pg_ctl.exe' -D 'C:\pg17\data' -l 'C:\pg17\data\pg.log' start
  ```

  كُتب هنا مرّةً أنها «تعمل كخدمة لا تُشغَّل يدوياً» فضاعت جلسةٌ كاملة على
  اعتقاد أن الحجب من Device Guard وأن لا حيلة فيه — **والخادم لم يكن مشغَّلاً
  فحسب**. ورسالةُ الحجب التي ظهرت مرّةً كانت على تشغيل `postgres.exe` مباشرةً؛
  و`pg_ctl` يمرّ. والإقلاع الأول بعد إطفاءٍ غير نظيف يأخذ نصف دقيقة في
  `syncing data directory` — انتظره ولا تظنّه معلّقاً.

  والتحقّق بالمنفذ لا بالمسار ولا بالخدمة:
  `C:\pg17\pgsql\bin\pg_ctl -D C:\pg17\data status`.
  كُتب هنا مرّتين خطأً — `D:\pgsql17` ثم `C:\pgsql17` — ولا وجود لأيّهما، فضاعت
  جلستان في البحث عن خادمٍ مثبَّت **وهو يعمل**. تحقّق بالمنفذ لا بالمسار:
  `Get-NetTCPConnection -State Listen | ? LocalPort -eq 5432`.
- `uv` **غير مثبَّت** على هذا الجهاز، فكل أمرٍ في `justfile` يبدأ بـ`uv run`
  يفشل بـ`command not found`. البيئة الجاهزة هي `backend/.venv` وتُستعمل مباشرةً:
  `backend/.venv/Scripts/python.exe manage.py …` من داخل `backend/`.
- خادم التطوير **بلا `--noreload`**: مع الفلاج لا يُعاد قراءة أي ملف بعد الإقلاع،
  فتُقرأ نسخةٌ ميتة ويُطارَد عطلٌ لا وجود له. حدث هنا مرتين.
- **المعاينة الثابتة ليست اللوحة.** `ops/preview_console.py` يكتب
  `dashboard.html` وحدها في `backend/.design-preview`، ويُقدَّم على `:8777`
  بخادم ملفات — فكلُّ رابطٍ في الشريط يُعطي **404**، وليس ذلك عطلاً في اللوحة.
  المعاينة لفحص **الشكل** بلا قاعدة بيانات؛ واللوحة الحقيقية بصفحاتها وبياناتها
  على `localhost:8001` (`backend-venv`). ولا تُرسَل `:8777` إلى المالك على أنها
  اللوحة — حدث، فقرأ الصفحاتِ ناقصةً وهي ليست كذلك.
- **الجلسة مربوطة بالمضيف حرفياً:** الدخول على `localhost:8001` لا يُعرَف على
  `127.0.0.1:8001` ولا العكس — كعكة الجلسة لكلٍّ منهما وحده. ثبِّت واحداً.
