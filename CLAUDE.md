# قواعد العمل في هذا المستودع

هذه قواعد **كيف يُشتغَل**، لا ماذا يُبنى. ما يُبنى في
`.specify/memory/constitution.md` و`docs/system-handbook.md`، والدستور يعلو على
هذا الملف عند أي تعارض.

## ١ — نفِّذ، ولا تستأذن في ما هو واضح

المالك طلب هذا بالحرف: «انجز ونفّذ التاسك بدون كلام كتير».

- التاسك المفتوح في `specs/*/tasks.md` **إذنٌ بتنفيذه**. لا يُسأل عنه مرة ثانية.
- السؤال يُطرح في حالة واحدة: قراءتان مختلفتان للطلب تُنتجان **عملاً مختلفاً**،
  ولا يحسم بينهما الكود ولا السجلّ. غير ذلك: اختر الأقرب، نفّذ، واذكر ما افترضت
  في سطر.
- «أبدأ؟» و«تحب أكمل؟» بعد جوابٍ سابق بنعم ليست أسئلة — إنما توقّفٌ بلا سبب.
- ما يُوقف العمل حقاً: حذفٌ أو كتابةٌ فوق شيء قائم، أو فعلٌ خارج الجهاز (دفعٌ
  إلى مستودع بعيد، إرسال، نشر)، أو تغييرُ إعداداتٍ ومفاتيح تكامل (المادة ٥-٤).

## ٢ — لا عملية بلا سبب

- **لا تُشغّل ما لا يتغيّر جوابه.** الحزمة الكاملة بعد تعديل سطرٍ في CSS ليست
  تحقّقاً؛ هي انتظار. شغّل ما يمسّه التغيير، والحزمةَ كلها قبل الدمج والدفع.
- **لا تُعِد قياس ما قِسته للتوّ.** إن كان الجواب في الجلسة فاستعمله.
- **افحص قبل أن تُصلح.** الأداة التي تكشف السبب أرخص من إصلاحٍ لسببٍ مُخمَّن —
  وقد كلّف تخمينُ «امسح الكاش» جلستين قبل أن يتبيّن أن الخادم يعمل بـ`--noreload`.
- **الأوامر المستقلّة تُرسَل معاً** في رسالة واحدة، لا واحداً في إثر واحد.

## ٣ — الردّ: النتيجة أولاً، والباقي عند الطلب

- ابدأ بما حدث. لا تمهيد، ولا سرد لما ستفعله، ولا إعادة لما فُعل.
- **الأرقام لا الصفات**: «١٣٧٥ اختباراً» لا «الحزمة خضراء»، و«نسبة ٣٫٠٦ والحدّ
  ٤٫٥» لا «التباين ضعيف».
- **قُل ما فشل ولماذا** بصراحة، ولا تُعلن إنجازاً لم يُقَس. وحدُ الأداة يُقال:
  «فحصٌ نصّي لا يثبت الشكل المرسوم» جملةٌ صحيحة ولازمة.
- لا تُكرّر ما في الرسالة السابقة، ولا تُذيّل بعرض خدماتٍ إضافية.

## ٤ — لكل إصلاحٍ حارسٌ، ولكل حارسٍ مخالفةٌ مصنوعة

هذه ليست قاعدة أسلوب: **حارسٌ يمرّ دائماً لا يُميَّز عن حارسٍ لا يعمل**، وقد
أخطأ حارسٌ هنا مرتين وهو صامت (نهايات CRLF، وآخر عنصرٍ بلا سطر بعده) قبل أن
يُجرَّب على مخالفة.

- عطلٌ يستحقّ الإصلاح يستحقّ اختباراً أو فحصاً في `ops/checks/` يمنع عودته.
- ويُجرَّب الحارس على مخالفةٍ مصنوعة، ثم تُرجَع المخالفة.
- الفحص الذي لا يجد ما يحرسه **يصرخ** ولا يمرّ.

## ٥ — الرسائل والتعليقات تقول *لماذا*

- رسالة الـcommit بالعربية، تقول العطل والسبب، لا قائمة ملفات.
- التعليق يشرح القرار وثمنه، لا ما يقوله السطر. وحين يُوجد عطلٌ في الإنتاج أو
  في v1 فاذكره — فهو ما يمنع «تبسيط» القاعدة لاحقاً.

## ٦ — الحالة تُقال كما هي

- `🟡` حالةٌ شريفة: معيارٌ لم يُقَس، أو تبعيّةٌ لم تصل. **لا تُعلَن ✅ بلا قياس**،
  ولو كان الكود مكتملاً.
- ما يحتاج جهازاً حقيقياً أو توقيع المالك يبقى مفتوحاً ومكتوباً في `tasks.md`.

## ٧ — البيئة

- PostgreSQL في `C:\pgsql17` يُشغَّل بـ`pg_ctl` (لا Docker)، ويسقط أحياناً:
  `C:\pgsql17\bin\pg_ctl -D C:\pgsql17\data -l C:\pgsql17\server.log start`.
  كان مكتوباً `D:` ولا وجود له هناك — فضاعت جلسة في البحث عن خادمٍ مثبَّت.
- خادم التطوير **بلا `--noreload`**: مع الفلاج لا يُعاد قراءة أي ملف بعد الإقلاع،
  فتُقرأ نسخةٌ ميتة ويُطارَد عطلٌ لا وجود له. حدث هنا مرتين.
- لكل تشغيل اختبارات قاعدةٌ خاصة عبر `TEST_DATABASE_URL` — تشغيلان متوازيان على
  قاعدة واحدة يتصادمان.
