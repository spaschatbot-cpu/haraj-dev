# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Secrets and local state — never committed
.env
*.log
media/
staticfiles/

# Flutter
mobile/build/
mobile/.dart_tool/
mobile/.flutter-plugins*

# Editors / OS
.idea/
.vscode/
Thumbs.db

# Worktrees the harness creates for parallel agents — never committed.
.claude/worktrees/

# معاينة اللوحة المرسومة محلياً (ops/preview_console.py) — مخرَجٌ لا مصدر.
backend/.design-preview/

# مهاراتٌ خارجية مثبَّتةٌ بـ`npx skills add` — أدواتُ جهازٍ لا كودُ مشروع.
# تُنصَّب بالأمر نفسه على أي جهاز، و`skills-lock.json` يثبّت النسخة.
.agents/
.claude/skills/
