"""Shared base models for every app.

Empty by design: the contents land in T006 (TimeStampedModel/UUIDMixin) and
T008 (AuditLog). This module exists now so `apps.core` is a real, importable
Django app from the moment the tree is restructured.
"""
